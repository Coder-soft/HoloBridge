version: '3.8'

services:
  holobridge:
    build: .
    container_name: holobridge
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - API_KEY=${API_KEY}
      - PORT=3000
      - DEBUG=${DEBUG:-false}
      - PLUGINS_ENABLED=${PLUGINS_ENABLED:-true}
      - RATE_LIMIT_ENABLED=${RATE_LIMIT_ENABLED:-true}
    volumes:
      # Mount plugins directory for hot-reload
      - ./plugins:/app/plugins:ro
      # Mount .env file
      - ./.env:/app/.env:ro
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
  # Optional: Redis for scaling (uncomment to enable)
  # redis:
  #   image: redis:7-alpine
  #   container_name: holobridge-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

  # volumes:
  #   redis_data:
